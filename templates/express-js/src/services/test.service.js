/**
 * Returns a string indicating that everything is up and running.
 * @returns {string} The status message.
 */
const testService = () => {
  return "everything is up and running!!";
};

module.exports = { testService };
